#include <stdio.h>

int main()
{
    int radius;
    printf("Enter radius: ");
    scanf("%d", &radius);
    printf("Area of circle is %.2f having radius %d", 22.0 / 7 * radius * radius, radius);
    printf("\n");
    return 0;
}
